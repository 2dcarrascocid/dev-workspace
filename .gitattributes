# Fuerza finales de línea Unix (LF) para scripts shell, sin importar la
# configuración de git de cada máquina (core.autocrlf). Windows suele
# convertir a CRLF en el checkout, lo que rompe bash con errores tipo
# "$'\r': command not found".
*.sh text eol=lf
